"""AI 어시스턴트 — 모델이 붙는 유일한 곳. 정본 specs/07-architecture/10-AI-어시스턴트.md

경계 셋이 여기서 지켜진다(§3).

  ① 권한   읽기는 전용 롤 bt_ai(master·ref 만) · 쓰기는 우리 API(x-ai 등급)
  ② 나가는 문  scrub() 한 곳. 사용자 입력과 도구 결과 **둘 다** 통과
  ③ 샌드박스  아티팩트는 오리진 없는 iframe

**프롬프트로 타이르는 자리를 한 곳도 두지 않는다.** 막는 것은 전부 코드와 DB 다.
"""
