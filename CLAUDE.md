# 빌탐정 — 작업 규칙

> 이 파일은 **세션마다 자동으로 읽힌다.** specs/ 문서는 찾아가야 읽히므로,
> 매번 지켜야 하는 규칙은 여기 둔다. 자세한 배경은 각 스펙 문서로.

## UI 어법 — 어기면 다시 만든다

**네모 입력칸·네모 버튼을 인라인 화면에 두지 않는다.** 폭을 통째로 먹어 옆에 정보를
세울 수 없고, 몇 개만 쌓여도 화면이 「읽는 곳」이 아니라 「채우는 서식」이 된다.
(정본: [specs/02-screens/네비게이션-규칙.md](specs/02-screens/네비게이션-규칙.md))

| 할 일 | 쓰는 것 | 쓰지 말 것 |
|---|---|---|
| 선택지가 있는 값(상태·담당자·등급) | **칩**(Chips) | `<select>` · 라디오 |
| 두 갈래 전환 | **슬라이딩 토글**(Segmented) | 체크박스 · 버튼 두 개 |
| 금액·시각 등 값 고치기 | **클릭-편집**(눌러야 입력이 열리고 벗어나면 닫힘) | 항상 떠 있는 `<input>` |
| 한 줄 기록 | **pill 입력줄**(CommitLog) | textarea |
| 항목 조작(완료·삭제·편집) | **아이콘 컨트롤**(작게, 줄 오른쪽) | 글자 네모버튼 나열 |
| 신규 등록 폼 | **모달 안에서만** 폼 허용 | 인라인 폼 |
| 검색 | 목록 상단 검색 pill | — |

- **줄마다 버튼을 나열하지 않는다.** 목록 다섯 줄에 버튼 두 개씩이면 버튼이 열 개다.
  조작은 아이콘으로 작게, 고른 줄에서만 보인다.
- **설명글씨 금지.** 차트·표 옆에 조건·요약을 붙이지 않는다. 남기는 건 제목·단위·축·값.
- 색은 뜻 하나씩. **2026-08-24 확정(토스 결)**: 유채색은 파랑 #3182F6 하나(선택·주인공 값·채택)
  + 빨강 #F04452(기한·경고·보류). 매도 보라 폐지. 회색 바탕 위 흰 카드, 선택=연파랑 바탕.
  줄 문법: 줄엔 라벨·현재 값만, 클릭하면 펼침, 선택 칩 재클릭=미지정. 표준 구현체 =
  통합 매물 모달(frontend/src/features/sales/draft/). 구 규칙은 [색-체계.md](specs/02-screens/색-체계.md).

## 데이터 원칙

- **미지정은 null.** 모르면 비워 둔다 — 기본값을 지어내지 않는다(없는 약속 시각을
  9시로 채우면 아무도 한 적 없는 약속이 화면에 선다).
- **프로필 + 커밋 장부.** 모든 개체는 신원(프로필)과 움직임(커밋)으로 나뉘고,
  현재 상태는 장부의 파생값이다([프로필-커밋.md](specs/04-data/프로필-커밋.md)).
- **커밋은 원문 그대로.** 파서는 칸만 채운다. 못 읽으면 **아무것도 안 옮긴다** —
  조용한 실패가 틀린 해석보다 낫다([일정-캘린더.md](specs/04-data/일정-캘린더.md)).
- 마이그레이션은 `db/migrations/NNNN_*.sql` 번호순. 적용은 `db/apply.sh`.

## LLM 안 쓴다 (2026-08-14 결정)

**커밋 해석·요약·대상 찾기 어디에도 LLM을 쓰지 않는다.** 규칙(정규식 파서)으로 한다.

- **개인정보** — 커밋 문장엔 고객 이름·전화·금액이 그대로 들어간다. 외부 API로 보내면
  제3자 제공이다. 중개인에게 「내 고객 정보가 어디로 가느냐」는 곧바로 걸리는 문제다.
- **신뢰** — 규칙은 못 읽으면 조용히 아무것도 안 한다. LLM은 **그럴듯한 오답**을 만든다.
  금액·날짜가 한 번 틀리면 그 뒤로 아무도 안 본다.
- **속도·돈** — 한 줄 치고 바로 넘어가는 맛이 이 제품의 강점이다. 1~2초가 붙으면 죽는다.

어휘가 모자라면 **회귀 코퍼스에 문장을 넣어 규칙을 늘린다**
(`frontend/src/features/sales/__parser__/`). 34% → 0% 를 그렇게 내렸다.

## 작업 방식

- **배포는 명시 지시 때만.** 로컬 검증까지가 기본이다.
- 화면을 고쳤으면 **스크린샷을 찍어 눈으로 확인**한다. DOM 개수만 세고 통과시키지 않는다.
- **QA는 `qa/run.sh` 한 문으로.** 바뀐 파일을 보고 필요한 것만 돈다. 커밋 전에 돌린다.
  목적별 폴더(`qa/README.md`): data(DB 불변식) · screen(화면이 DB를 맞게 읽는가) ·
  mirror · app · build · ui · external(토지이음 대조, 손으로만).
  **건너뛴 것은 통과가 아니다** — 서버가 안 떠 있으면 그렇게 말하고 넘어간다.
- 파서를 고쳤으면 회귀를 돌린다: `cd frontend && npx vite-node src/features/sales/__parser__/run.ts`
- 거울(커밋↔일정↔참석자↔가격)을 건드렸으면: `qa/run.sh mirror`
  — 거울 동사는 `backend/app/domains/mirror.py` **한 곳**에만 쓴다. 핸들러 인라인 복제 금지.
- 화면을 고쳤으면 `qa/run.sh screen ui`. 새 화면은 `qa/screen/qa_screen.py` 에 **경로 한 줄**만
  넣어도 흔적 찾기가 붙는다. 기대값은 그 값이 문서로 나갈 때만 박는다.
