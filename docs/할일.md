# 할일

> 대표가 준 순서대로 한다. 끝난 것은 상태를 적고 지우지 않는다.
> **2026-09-05 정리** — 항목이 세 벌씩 겹쳐 있던 것을 한 벌로 합쳤다.

## 지금 남은 것 (2026-09-07 저녁 정리)

**대표가 정할 것**
1. **커밋** — 09-06 밤~09-07 낮 작업 전부 미커밋(파일 150개 안팎). 목록은 「오늘 밤 만든 파일」 절.
2. **배포** — 전부 로컬만이다. 마이그레이션 0157~0162 · 새 대장 판 · 공시지가 3,456만 · 검증 91 · 화면 수정 전부.
3. **크롤 자동화** — 스케줄러 없이 손으로 부른다(기존 결정). 받기 16개는 **배선만 하고 실제로 안 돌렸다**
   (실제 받은 것은 고시·보도자료·나라장터·상가정보 넷). 주기 돌아올 때 첫 실행에서 잡을지, 가벼운 것부터 지금 돌릴지.
4. **디스크** — 여유 16GB. 다음 대장 빌드에 45GB. 도커 이미지 69GB·바탕화면 다른 프로젝트 17GB 정리는 대표 판단.
5. **표제부 원문 48키 jsonb 부록 표**(09 스펙 §6 제안) — 화면엔 안 보이되 AI 용으로 보유. 필지 쪽(용도지역 비중·개발제한비중)도 같은 안. 승인 대기.
6. **2차 설문 링크는 프로덕션 주소로** — 1차 응답 셋이 전부 개발 사이트(`bt-api-dev`)로 들어왔다. 프로덕션 표는 0건.

**코드로 할 것(우선순위)**
- ~~`run_pipeline.sh` 되붙임 셋 → 일곱~~ **끝냄(09-07)** — 단위 실행기와 옛 셸 두 길이 같은 일곱을 건다(필지 용도지역·도로폭·층별개요·필지 연결·승강기·공시지가·마지막 매각).
- **M-③ 활용유형(`_use_type`)을 대장 용도 대신 실제 업체(상권 7갈래)로** — `core/market.py STORES` 는 있고 점수 배치만 아직 대장.
- ~~`parcels.reg_*` 죽은 칸 걷어내기~~ **끝냄(09-07)** — 칸 **일곱**(규제 여섯 + `uqa`)을 뿌리까지. 백엔드 7·프론트 6·나대지 1·로더 3·내보내기 3 + **0163 으로 표에서 DROP**(옛 세대까지, 뷰·나대지 MV 재생성). 정본은 `regulations` 하나. 곁다리: `ref.fields` 의 use_zone 물리 위치가 0행짜리 `parcels_v1.uqa` 를 가리키던 것도 바로잡음. 화면 불변.
- **데이터 정직성 셋**(09 스펙 §5 buildings 이상 1·9·11): 연·월만 아는 사용승인일을 export 가 버림 · 0 이 NULL 로 · 대수선일 연도만 알면 0101 로 채움.
- **`sanggwon` 원본 SHP 재수령**(공공데이터포털 15086933) — 지금 정본이 자기 백업 gz 다.
- **세대·백업 표 정리** — `buildings_v8`·`parcels_v8`·`_bak_0829`·`_resnap` 등. 도커 안 24GB.
- 층 모르는 업체 편입 — 오른쪽 판 + 「층 선택하기」 라벨로 확정(09-07). 더 손볼지는 써 보고.

**미정(아이디어)**: 소상공인 상가정보로 앵커 테넌트 신호 · 국민연금은 동 단위만 · 필지 `building_pk` 다동이면 마지막 동만 남는 것(09 스펙 parcels 이상 6).

### 2026-09-06 밤 대표 추가
- **원천별 증분 파이프라인** — 계획 `specs/07-architecture/07-원천별-증분-파이프라인.md`. ① 소식 여섯 단위 분리 → ② 장부·지문 → ③ 월·분기 단위 → ④ 대장 단위(디스크 가드)에서 C → ⑤ 문서.
- **고시문 PDF 글자 단계는 뺀다** — 읽는 화면·API 가 없다(실측). 파이프라인 derive 에서 제거, 스크립트는 남김.

### 2026-09-06 저녁 대표 추가
- **층별 임대정보 디자인안** — **그림(09-06)** https://claude.ai/code/artifact/fe67d5f8-5058-4e5b-b534-8d6ee17e2fdd
  **A안 확정(09-06 밤)** + 대표 수정: 왼쪽은 업체마다 한 줄(「1층 1층 1층」) · 층 모르는 업체는 층 「—」 줄(밸류맵식, 「층 미상」 말 안 씀) ·
  상태 열 없음 · ＋ 층은 카드 머리 · 개업일 삭제 · 머리엔 총면적(대장) 하나(「업체 4」·「대장」·남은 면적 없음) · 지하 호실도 「—」. 층 잘못 지정은 ⋯ 「층 지우기(—로) · 다른 층으로」.
  **옮겼다(09-06 밤, 대표 「일단 이렇게 바꾸자」)** — `FloorRows.tsx`: 왼쪽 업체마다 한 줄(층·업체·업종), 층 모르는 업체는 「—」 줄 맨 위,
  「—」 줄을 누르면 층 칩으로 보내기, 팀 줄엔 「다른 층으로」(지도 아이콘 → 층 칩)·지움, 머리에 「+ 층 추가하기」·「전체 되돌리기」,
  오른쪽 아래 「+ 호실 추가하기」, 층 머리엔 총면적 하나, 개업일 삭제(`/tenants` 도). 지운 줄은 원장에 있으면 다시 「—」 줄로 돌아온다.
- **지도 소식 필터 디자인안**(09-06 밤, 대표 「각 아이콘이 뭘 뜻하는지 알아야지 · 디자인안 뽑아」) — 아이콘만 있는 칩은 뜻을 모른다.
  안: 「소식」 버튼 하나 → 판(기간 칩 + 종류 아홉 줄: 아이콘·이름·화면 안 개수·스위치). 사이드바도 같은 판, 목록 줄 앞에 같은 아이콘.
  https://claude.ai/code/artifact/58351c1a-90ea-4907-bf53-95fc1c3a0b79 — **옮겼다(09-06 밤, 대표 「오키 이렇게 진행」)**.
  `NewsFilterPanel`(검색 지도·사이드바 공용): 「소식」 버튼 → 판(기간 칩 · 종류 줄 아이콘·이름·화면 안 개수). 줄을 누르면 켜고 끔, 켜진 줄은 파랑(스위치 없음).
  사이드바는 반경 안에 있는 종류만·기간은 주변 소식 칩과 공유. 목록 아이콘엔 이름 title.
  핀 호버 말풍선은 마커 요소에 `mouseenter` 를 직접 건다(네이버 HTML 마커는 `mouseover` 가 안 온다). QA screen 17 ✓.
- **말풍선 「소식에서 보기」·「출처」**(09-06 밤, 대표) — 이름 검색으로 소식 탭만 열리던 것 → `/news?pick=<area_event id>` 로 **그 소식**을 연다
  (`GET /news/item?id=` · 목록에 없어도 오른쪽에 선다). 「출처」는 원문(고시문·보도자료·공고) 새 탭. 사이드바 말풍선도 같은 두 링크.
- **주변 소식 줄 깨짐**(아이콘 넣으며 두 칸 격자에 세 칸이 들어가 날짜가 다음 줄로) — `.ae-i` 를 아이콘·이름·날짜 세 칸으로.
- **소식 종류 칩·목록 아이콘**(09-06 밤, 대표) — 「주요/전부」 토글 대신 **종류 아홉을 각각 켜고 끄는 칩**(`EventTypeBar`, 기본 주요 일곱:
  크레인·학교·철도·공원·공사 발주·철거·정책 발표 / 꺼짐: 신축·증축·규제·기타). 검색 지도·사이드바 지도 둘 다(`useAreaPick.types`).
  소식 탭·주변 소식 줄 앞에도 같은 아이콘. 사전은 `eventIcon.ts` `EVENT_TYPES` 한 곳.
- **층별 임대 다듬기**(09-06 밤, 대표) — 「다른 층으로」(지도 아이콘→층 칩) 뺐다(「이미 그 층인데 왜 다른 층이 나오냐」): 잘못 넣었으면 지우고
  「—」 줄에서 다시 고른다. 「1번째·2번째」 순번 안 세움 — 이름 없는 줄은 「—」. 층 추가 입력 자리표시 「예: 3층」.
- **소식 핀 호버·기간**(09-06 밤) — 마우스만 대도 말풍선(누르면 고정). 지도에도 1년(기본)·3년·5년·전체 칩: 검색 지도는
  `GET /news/pins?years=`(0=전체), 건물 사이드바는 주변 소식 칩과 같은 값(`useAreaPick.years`).
- **소식 아이콘을 건물 검색 지도에도** — **끝냄(09-06)**. `GET /news/pins?bbox`(자리 아는 것, 최근 1년, 400개까지) →
  `MapPanel` 이 idle 마다 받아 찍는다(줌 13 아래선 안 찍음). **기본 「주요」는 신축 인허가를 뺀다** — 강남 한 화면 163개 중 152개가
  신축 인허가라 건물 핀을 덮었다(`isMajor`, 사이드바는 반경 250m 라 그대로). 「소식 전부」 토글은 오른쪽 아래 도구 줄 위.
  핀을 누르면 말풍선(이름·갈래·날짜·출처·「소식에서 보기」). `.ev-pin` 스타일과 `eventIcon` 은 shared 로 옮겼다.
- **매력도 기능 삭제** — **끝냄(09-06)**. 보고서 슬라이드 03·요약 링·종합결론 줄·서사, 검색 pill, 검토 모달 「가치점수」, 매수자 카드 등급,
  건물 상세 API(score·grade), `value_score.compute`, 배치(`build_building_score`), 0161(`building_score` score·grade·items 삭제), QA 검사, 베타 안내문.
  적정가 산식(appraise)은 점수를 안 쓰고 있어 값이 안 변한다. 활용유형·매도가능성·미래가치는 그대로.


### ① 층별 임대정보에 업체 원장 흡수 (항목 L) — **끝냄(09-06)**
### ② 주변 소식 화면 — **끝냄(09-05)** · 갈래 다섯(09-06)
### ③ 호재 표 — **끝냄(09-05)** · 이름은 `master.area_event`
### ④ 소식 탭 — **끝냄(09-06)** · `/news` · 서울 전체를 자리 없이 날짜순

넷 다 끝났다. 남은 것은 적재·재빌드라 **파이프라인을 같이 돌려야 한다**(항목 C).

---

## C. 파이프라인 재적재 — **끝냄(09-07)** · `--unit ledger` 완주 · 승강기·필지·교통까지 단위로

**단위 분리·장부는 됐다(09-06 밤).** `pipeline/units.py`(단위 12·파생 9 선언) · `scripts/run_unit.py`(실행기) ·
`0162_source_version.sql`(장부 둘). 이제 주간 소식은 대장 전체 빌드 없이 혼자 돈다.

    scripts/run_unit.py --list                 # 판·마지막·줄수 · 갱신 필요한 파생
    scripts/run_unit.py --cadence weekly       # 고시·보도자료·나라장터 + 주변 소식 합침
    scripts/run_unit.py --unit ledger          # 전체 빌드(여유 40GB 미만이면 시작 안 함)

첫 실주행(`--cadence weekly`, 38.9분)에서 **묵은 고장 둘**이 같이 나왔고 고쳤다:
- **적정가 배치 TypeError** — 매력도 제거로 `appraise()` 인자를 줄였는데 부르는 쪽 셋을 안 고쳤다.
  보고서 화면은 멀쩡했고 배치만 죽어 있었다. 화면이 돈다고 배치가 도는 게 아니다.
- **필지 원장의 용도지역·법정 건폐·용적이 0건** → `building_legal` 0줄 → 검색 법정 필터 전멸.
  `load_parcel_luris.py` 를 실어 용도지역 896,164 · `building_legal` 562,886줄(94.7%).
  이 되붙임은 필지 재적재마다 같이 돌아야 한다 — 대장 단위 적재 줄에 넣었다.
- `fill_building_parcels.py` 실행 — 건물↔필지 구멍 **57,228 → 9,253**(남은 건 필지 원장에 지번이 없는 건물).
- 검증: `qa/run.sh data screen ui` 통과 3 · 실패 0 · 화면 17 ✓.

**대장 전체 빌드·적재·파생 전부 끝냈다(09-06 밤).** 빌드 28단계 1,555초 · 적재 11표 · 파생 9 · QA 3/3 · 화면 17 ✓.
buildings 585,731 · parcels 용도지역 896,164 · building_legal 562,886(94.7%) · gongsi 1990~2026 2,840만 ·
building_parcels 구멍 57,228 → 9,253 · area_event 23,380.

### 2026-09-07 낮 — 원천 스무 곳 중 열아홉이 자기 단위로 선다
안 되던 여섯을 전부 뗐다: `road_width`(8분) · `living_pop`(0.6분) · `trade_area` · `transit`(4분) ·
**`parcels`(13.5분 — 공간조인부터 토지이용계획 되붙임까지 대장 빌드 없이)**. 토지이용계획은 parcels 안에서 같이 풀린다.
`news.permit` 만 받기가 없는데 건축HUB 압축본 안이라 `ledger` 받기가 같이 가져온다(의도).
- 되붙임 새로 둘: `fill_transit.py`(역거리·주변교통) · `fill_sale_latest.py`(마지막 매각 — 90일 묵던 자리). `fill_gongsi_latest` 는 필지도 덮는다.
- 받기 실제 확인: 소상공인 상가정보 344MB zip → `activate` 가 서울 CSV 만 `_sbiz` 로(새 규칙). `news.zones`·`elevator` 받기 뒤에 `activate` 마디.
- `export_parcels.py --pk-from-db`: 필지 단위가 SQLite 없이 살아 있는 DB 에서 건물 PK 를 가져온다.
- 함정: **psycopg 쓰는 스크립트는 gis venv**(data/.venv 가 상위집합 · backend/.venv 엔 asyncpg 만). 셋이 여기서 죽었다가 고침.
- 장부 19단위 · 검증 91 · `qa/run.sh units screen data mirror` 전부 통과. 디스크 여유 16GB.

### 2026-09-07 — 칸별 사슬 정본과 이상 37건
`specs/07-architecture/09-표별-칸별-사슬.md` — 표 52 · 칸 525 전부. 원천 스무 곳 중 단위 열넷.
**고칠 것(우선순위):** ① `run_pipeline.sh` 되붙임 셋 → 여섯(셸로 대장 실으면 building_parcels 47,975줄 유실)
② 표제부 원문 48키를 jsonb 부록 표로 **보유**(화면 미노출은 의도 · 데이터는 AI 용 — 09 §6) ③ `last_sale_ym/price`·`parcels.gongsi_latest` 되붙임
④ `parcels.reg_*` 여섯 죽은 칸 — 스키마·쿼리에서 걷어내기 ⑤ `building_pop` 자동 호출 ⑥ `sbiz_store` 크롤 함수 ⑦ `trade_area`·`subway_stations` 크롤↔적재 경로 잇기.

### 2026-09-07 — 오늘 밤 만든 파일 (커밋 안 함 · 미추적)

| 파일 | 무엇 |
|---|---|
| `pipeline/units.py` | **선언 정본** — 단위 14 · 파생 9 · 검증 67 · 되붙임 배선 |
| `scripts/run_unit.py` | 실행기 — 네 마디(크롤·빌드·적재·검증) · 장부 · 낡음 판정 |
| `db/migrations/0162_source_version.sql` | 장부 둘 — `source_version` · `derive_run` |
| `scripts/build_gongsi_series.py` | 공시지가 빌더(D150 dbf 에서 바로) |
| `scripts/load_elevator_ext.py` | 승강기공단 되붙임 |
| `scripts/fill_gongsi_latest.py` | 공시지가 되붙임(`buildings.gongsi_latest`) |
| `scripts/fill_building_parcels.py` | 건물↔필지 구멍 메우기 |
| `qa/data/qa_units.py` | 선언을 읽어 전부 훑는 QA |

고친 것: `pipeline/loader.py`(행수 문턱을 지난 적재분과 견줌) ·
`data/tools/build_integrated.py`·`build_sqlite.py`·`pipeline/export_seoul.py`(승강기 사슬) ·
`backend/app/jobs/report_calc.py`·`scripts/rent_estimate/build_sale_est.py`(appraise 인자) ·
`scripts/build_area_event.py`·`scripts/load_building_permit.py`·`backend/app/domains/tenants.py`(세대 이름 제거) ·
`qa/run.sh`·`qa/screen/qa_screen.py`(units 검사 · 승강기 화면 검사) ·
`specs/07-architecture/03·06·07`(문서를 단위 기준으로).

### 2026-09-07 밤 — 크롤·적재·검증을 주기별로 분리

대표 지시: 「크롤부터 적재 후 최종검증까지를 각 데이터의 주기에 맞게 분리. 빌드만 나누는 게 아니라.」

**한 단위가 네 마디로 돈다.** ① 크롤 ② 빌드 ③ 적재 **④ 검증**. 넷째가 없었다.
검증이 깨지면 판을 안 올리고, 판이 안 오르면 그 단위를 읽는 파생도 안 돈다.

    scripts/run_unit.py --unit gongsi        # 「오늘은 공시지가 하는 날」
    scripts/run_unit.py --unit news.press    # 「오늘은 보도자료 하는 날」
    scripts/run_unit.py --cadence weekly     # 그 주기 것 전부
    qa/run.sh units                          # 전부 한 번에 훑기

**검증 60가지를 단위·파생이 스스로 선언한다**(`pipeline/units.py` 한 곳).
단위 34 · 파생 15 · 받은 것 확인 11. `qa/data/qa_units.py` 가 그 선언을 읽어 돌린다 —
검사문을 두 곳에 적으면 곧 어긋나므로 정본은 `units.py` 다.

**받은 것 확인이 새로 생겼다.** 파일 개수·크기 하한·첫 4KB 머리글을 크롤 직후에 잰다.
실거래 2016년이 1,006바이트 HTML 오류 페이지로 받아진 것을 나흘 뒤에야 안 사고를 그날 잡는다.

**공시지가를 뗐다**(본보기). `scripts/build_gongsi_series.py` 신규 — D150 dbf 에서 바로 뽑는다.
전엔 `build_land_master` 안의 한 함수라 공시지가만 갱신하려 해도 대장 28단계를 다 돌았다.

**승강기도 뗐다.** `scripts/load_elevator_ext.py` 신규 · 분기 단위. 대장 적재 뒤에도 다시 붙는다.

**(09-07 낮 끝냄 — 아래는 그날 밤 기록)** 아직 못 뗀 단위 둘. 필지(반기)는 공간 조인·부속지번·SQLite 넷에 걸려 있고,
교통(반기)은 자기 표가 없이 `buildings` 의 칸이다. `spatial_join` 을 먼저 떼야 풀린다.

**(09-07 낮 끝냄 — 114,423동) 남은 것 하나: 승강기 참조값 `elevator_ext` 0건.** 「마스터를 다시 빌드하면 채워진다」는 **틀렸다** —
다시 빌드해도 0 이었다. 진짜 원인은 `build_sqlite.py` 의 `buildings` 표에 `엘리베이터참조` 칸이 없고
`export_seoul.py` 가 「칸이 없으면 빈칸」으로 봐준 것이다(585,731동 전부 조용히 빈칸).
**코드는 고쳤다.** 값은 다음 대장 빌드에서 찬다 — **여유 45GB** 가 필요하고 지금은 12GB.

**디스크.** 2차 빌드에서 여유가 0 이 되어 Postgres 가 죽었다(자동 복구 · 손실 없음). 도커는 디스크가
모자라면 안 켜진다. 캐시 39GB 를 비워 되살렸다(크롬 7.4 · pip 4.6 · 펼친 HUB 22 · SQLite 3.2 · 설치본 2).
원천은 하나도 안 지웠다 — **공시지가 원본 11판은 `build_land_master` 가 직접 훑으므로 지우면 2016~2026 이 통째로 빠진다.**


09-06 밤 `STEP=build` 전체 빌드를 띄웠다가 **디스크가 0** 이 됐다(빌드 전 여유 21GB · 빌드가 HUB 압축본을 11GB 로 펴고 중간 jsonl·SQLite·CSV
25GB 를 겹쳐 쓴다). 재부팅 뒤 사본을 지워 37GB. 아무것도 적재되지 않았다(적재는 검증 뒤 스왑).
대표 지적 「항상 전체 빌드가 문제 · 데이터별 주기대로 개별 빌드 · 버전 관리 · 소식은 더 세분화」 →
**계획: `specs/07-architecture/07-원천별-증분-파이프라인.md`** (단위·주기·장부·디스크·순서 ①~⑤). C 는 그 ④(대장 단위)에서 한다.
- 로더 `swap_view`: `CREATE OR REPLACE VIEW` 가 열이 바뀌면(elevator_ext 가 중간에 낌) 실패하던 것 → 딸린 구체화 뷰
  (region_index·vacant_parcels)의 정의·색인을 받아 두고 CASCADE 로 지운 뒤 다시 세운다. 임시 표로 시험함.
- `scripts/fill_building_parcels.py`: 연결이 없는 건물에 자기 대지 pnu 를 「대표」로. 구멍 57,228 중 47,975(84%)가 필지 원장에
  실재하는 pnu 다(총괄표제부 없는 단독 표제부 건물). 나머지 9,253 은 좌표도 필지도 없는 건물. **적재 뒤에 돌리고 derive 에 붙인다.**
- 고시문 글자 단계는 한 번에 400개(`BT_PDF_LIMIT`).
- 업체·상권 갱신 배선은 이미 파이프라인 안에 있다(crawl monthly/quarterly + derive load_sbiz·load_localdata) — 이번 실행이 곧 갱신 검증.

혼자 안 한다. 여기서 한꺼번에 처리할 것:

- ~~승강기 값이 아직 안 갈렸다~~ **끝냄(09-07)** — `elevator` 단위 · 114,423동 · 대장이 있어도 늘 옆에
- **`master.buildings` 뷰가 컬럼을 나열하는 뷰**라 `elevator_ext` 가 뷰에 없다. API 가 못 본다.
  뷰를 다시 만들려면 `DROP VIEW CASCADE` 가 필요하고 그러면 `app.search_polygon` 이 같이 날아간다
- `building_legal`·`district_plan` 이 derive 단계에 새로 들어갔다(로컬은 돌려 놨다)
- **`building_parcels` 구멍 57,228동** — pnu 는 있는데 필지 연결이 없다. 원인을 여기서 본다
- 아래 적재 건들(I·K·E-2·E-3)도 여기서
- **상권 갈래를 대장 용도에서 실제 업체로**(항목 M) — I·K 가 들어오면 바로 갈아끼운다

---

## E. 주변 소식(호재)

### E. 화면 — **확정(09-05)**

**정본:** https://claude.ai/code/artifact/c262c0df-f321-4895-9cc4-4d97291d5b5b
입지 탭 사이드바 셋째 항목. **왼쪽 목록 · 오른쪽 상세**(층별 임대와 같은 어법).

```
주변 소식                              [전체] 3년  5년
정비 · 개발  8                  홍실아파트 주택재건축 정비사업
  홍실아파트 주택재건축 정비사업 25.01   [지구단위계획]
  국제교류복합지구 지구단위계획구역 24.11   고시일    2025년 1월 9일
  청담 지구단위계획구역          24.03   고시번호  —
  …                                   출처     국토교통부 국가공간정보
기반시설  0
건축 인허가  0
```

**갈래는 용어로.** 「더 지을 수 있게 된다」는 설명이지 이름이 아니다.
- **정비 · 개발** — 정비구역 · 재정비촉진지구 · 지구단위계획 · 도시개발구역
- **기반시설** — 학교 · 철도 · 정류장 · 공원 (국토계획법상 기반시설)
- **건축 인허가** — 옆 필지 신축 · 대수선 · 철거

**지도가 거리를 말한다.** 목록에서 고르면 오른쪽 지도의 아이콘에 파란 테와 파문이 돈다.
반대로 지도 아이콘을 누르면 목록이 그리로 간다. **거리 열은 안 쓴다** —
「구역 안」·「73m」·「같은 법정동」이 한 칸에 안 들어간다. 주소를 아는 것은 전부 지도에 찍는다.

**아이콘 일곱** (1.7px 단색 선 · 쓰는 자리의 글자색을 따른다)

| 무엇 | 아이콘 | 새로/기존 |
|---|---|---|
| 정비 · 개발 | 크레인 | 새로 |
| 학교 | 학사모 | 새로 |
| 철도 · 정류장 | `subway` | 기존 |
| 공원 | 나무 | 새로 |
| 신축 | `building` | 기존 |
| 철거 | 철거 | 새로 |
| **기타** | `flag` | 기존 |

「기타」가 필요하다 — 고시에는 갈래로 안 갈리는 것이 섞여 있다(공원조성계획·도로구역·경관지구 변경).
억지로 여섯 중 하나에 밀어 넣으면 뜻이 틀린다. 깃발 하나로 두고 이름을 읽게 한다.

**표기 규칙**
- 목록은 **이름이 앞·크게**, 날짜는 `25.01` 로 짧게. 누르면 상세에 `2025년 1월 9일` 로 전부
- 이름은 그대로, 빈 이름은 고시번호(`서울특별시 고시 제2022-406호 정비구역`)
- **빈 갈래엔 아무 말도 안 쓴다.** 이름과 `0` 이면 끝
- 화면 글자에 **우리끼리 쓰는 말을 안 쓴다** — 「원장」·「원천」(→ **출처**)·「반경 안」.
  출처 값도 `V-World C_UQ161` 이 아니라 **국토교통부 국가공간정보**

### E-1. AI 에이전트가 읽는 표 — **화면보다 이게 먼저다**

화면은 이 표를 그리는 한 가지 방법일 뿐이고 **같은 표를 에이전트가 그대로 읽는다.**

```
building_pk  1024123619
kind         정비·개발            ← 코드. 문장이면 모델이 뜻을 다시 해석한다
name         홍실아파트 주택재건축 정비사업   ← 그대로. 우리가 지은 제목이 아니다
on_date      2025-01-09          ← 없으면 null. 「2025년」을 2025-01-01 로 채우지 않는다
on_year      2025                ← 연도만 아는 경우를 담는 칸이 따로 있다
gosi_no      null                ← 모르는 것은 null. 빈 문자열·0·「미상」이 아니다
relation     반경 안              ← 구역 안 | 반경 안
distance_m   412
body         (고시 본문)          ← E-2 에서 채운다
source       국토교통부 국가공간정보
source_url   https://urban.seoul.go.kr/…/고시.pdf
```

셋을 지키면 지어내지 않는다. ① **갈래는 코드로** ② **모르는 것은 null** ③ **출처를 값 옆에.**
날짜를 `on_date`/`on_year` 두 칸으로 가른 것도 같은 이유다 —
한 칸에 뭉개면 모델이 `2025-01-01` 을 실제 고시일로 읽는다.

### E-2. 고시 본문 — **적재 끝(09-05)** · 고시문 글자는 재추출 중(09-06)

`scripts/urban/fetch_notices.py` · 서울도시공간포털

```
총 43,989건 · 본문 빈 것 0 · 중앙 280자 · 최대 2,017자 · 고시문 PDF 99%
title · content · noticeNo · noticeDate · site · charger · phone · noticeCode
```

**결합률 실측** — 「PDF 99%」는 **그쪽 고시** 100건 중 99건에 PDF 가 있다는 뜻이지 우리 결합률이 아니다.

| 우리 것 | 붙나 | 방법 |
|---|---|---|
| 지구단위계획 948 | **100%** (60/60) · 본문 100% · PDF 98% | `ntfc_sn` = `noticeCode` **1:1** |
| 정비구역 839 | **48%** (19/40) | 이름 검색 |

정비구역이 안 붙는 이유는 대부분 표기 차이 — 「청량리8구역」 vs 「청량리 제8구역」 ·
「…정비구역」 vs 「…정비사업」. 정규화하면 오르지만 「개봉5주택재건축-자세한사항은 주택과 문의」처럼
이름 자리에 안내문이 든 것은 못 고친다. **100% 는 못 간다.**
화면은 본문이 있는 것만 보이고, 없으면 그 칸을 안 만든다.

**포털엔 구역 목록 API 도 따로 있다** — 도시계획시설 **19,647건**(`ctymgrpln/getUbplfcList.json`) ·
용도지구 147 · 용도구역 70. 도시계획시설 한 건에 `zoneName`(학교) · `locationName`(도봉구 도봉동 381-1) ·
`areaAfter`(면적) · `dNoticeCode`(고시 연결)가 온다. **이게 기반시설 갈래의 원천이다** —
V-World `UQ164` 를 따로 받는 것보다 낫다(주소·면적·고시가 처음부터 붙어 있다).

**함정:** `/ntfc/getNtfcList.json` POST+JSON(`/api/ntfc/…` 는 404) · Referer 필요 ·
PDF 경로는 `UpisArchive`(대문자 U·A). `upisArchive` 는 404

### E-3. 나라장터 공사 입찰공고 — **적재 끝(09-05)** · 자리 아는 242 만 지도에

`scripts/g2b/fetch_bids.py` · 키는 `scripts/.env.pipeline` 의 `G2B_KEY`(gitignore)

정비·지구단위는 고시로 남지만 **역 출입구 개선·보행환경 개선·문화재 보수는 어떤 고시에도 안 남는다.**
도시관리계획을 다시 결정하지 않기 때문이다. **공사 발주로만 잡힌다.**

실제로 잡힌다(2026-03~09 공사공고 12,987건).

```
강남대로 외 4개소 보행환경 개선 공사    서울 서초구   2026-03-16
종묘 주변 등 4개소 원형관로 보수공사    서울 종로구   2026-03-31
성수역 외 1역 화장실 개량 건축공사
```

**결합률이 낮다.** 서울 발주 909건 중 공고명에 우리 도로명이 있는 것 **2%**, 역명 **5%**.
대부분 「골목길 개선사업」처럼 권역·구 단위라 지명이 없다.
**붙는 것만 붙이고 나머지는 버린다** — 동 단위로 억지로 붙이지 않는다.

**함정:** 조회 기간을 넓게 잡으면 `resultCode 07 입력범위값 초과`. **15일씩 끊는다.**
날짜는 `YYYYMMDDHHMM` · `type=json` 필요 · 응답 칸 143개 중 값 있는 것 60개쯤

### E-4. 호재 원천은 **여섯**이다

「포털 하나로 정리된다」고 했던 것은 취소한다. 각자 다른 것을 덮는다.

| 원천 | 무엇을 | 상태 |
|---|---|---|
| 서울도시공간포털 | 정비·개발 · 기반시설 · **고시 본문·PDF** | 적재(09-05) |
| 건축HUB | 옆 필지 신축·대수선·**철거** | 적재(09-05) · 건축 인허가 14,946 |
| 나라장터 | **기존 시설 개선** | 적재(09-05) · 자리 아는 242 · 기반시설 안에 |
| 서울시 보도자료 | **정책 발표**(발표~발주 사이 1~2년) | 적재(09-06) · 2,039줄 · 본문 없음(4유형) |
| 서울 열린데이터광장 SHP | 도시계획시설 · **개발행위허가제한=규제** | 적재(09-05) |
| V-World | 폴리곤 도형 | 일부 보유 |

**팀원 자료(종로5가 93-1) 일곱 중 넷이 된다.** 정비구역 셋·세운4구역은 폴리곤에서,
종로4·5가·율곡로 지구단위계획은 고시에서. 나머지 셋(역 출입구·보행친화거리·문화재)은
E-3 로만 잡히고, 그것도 **발주 전이면 못 잡는다.**

**못 하는 것** — 계획 단계(고시 전 「수정가결」)는 어떤 원장에도 없다. 팀이 적는 칸을 둔다.
뉴스는 저장이 약관 밖이라 화면 열 때 링크만 세우고 버린다.

---

## L. 층별 임대정보에 업체 원장 흡수 — **끝냄(09-06)**

**한 것** — `GET /buildings/{pk}/tenants`(`backend/app/domains/tenants.py`): LOCALDATA 영업 중(좌표가 이 건물 필지 안) +
소상공인(pnu) 을 이름으로 접는다(「(주)」·괄호·띄어쓰기·끝 「점」 지우고, 세 글자 이상 접두까지). 상호명·층·면적·개업일·업종·전화.
`FloorRows.tsx`: 왼쪽 층 줄의 상호명은 팀 값 → 없으면 원장, 프리필 줄에도 원장 이름이 미리 선다(팀이 손대면 팀 줄로 굳는다).
오른쪽 상세 아래에 그 층 업체 줄(이름 · 업종 · 면적 · 개업 · 전화). 카카오는 화면에서 이름으로 붙어 전화·링크만 채우고
못 붙은 것은 **「층 미상 N ›」** 로 맨 아래(저장 안 함). 「업체 정보」 카드와 목차 줄은 지웠다.
삼성동 78: 원장 28 + 카카오 2 → 층 아는 것 10 · 층 미상 20 · 전화 9.
**남은 것** — 갱신(LOCALDATA 매일·소상공인 분기)은 파이프라인(C)에 붙인다. 폐업은 LOCALDATA `state` 로 이미 걸러진다.


**「업체 정보」 칸을 없애고 층별 임대정보 하나로 모은다.**
화면안: https://claude.ai/code/artifact/0b4e2a16-8814-419e-bf0c-00b5fae8274f

| 원천 | 저장 | 무엇을 채우나 |
|---|---|---|
| LOCALDATA 인허가 | 담는다 | **주력** — 상호명 · **면적** · 개업일 (층 80.7% · 면적 99.8%) |
| 소상공인 상가정보 | 담는다 | **보완** — 인허가 대상 아닌 업종(컨설팅·디자인). 층 68% |
| 카카오 로컬 | **안 담는다** | **전화번호.** 화면 열 때 받아 그 자리에서만 |

**카카오는 이름으로 붙인다.** `괄호 · (주) · 띄어쓰기 · 끝의 「점」` 을 지우고 접두까지 맞춘다.
서울 8개 건물 실측 — 카카오 64곳 중 **33곳(52%)** 이 붙고, 전화 있는 37곳 중 **18곳(49%)** 이 층까지 달렸다.

**안 붙는 것은 버리지 않는다.** 목록 맨 아래 회색 한 줄 — `층을 모르는 업체  8  ›`.
**주황 점선과 「카카오」 배지는 안 쓴다**(주황은 우리 색이 아니고, 층이 아닌 줄이 임대상태 칸을 흉내 내면 흐려진다).

**순서**
1. ~~층 목록 스크롤(접기·더보기 폐지)~~ **끝냄** `86cd9c4`
2. **카카오 흡수 + 전화 매칭 + 「층을 모르는 업체」** ← 프론트만. 지금 할 수 있다
3. LOCALDATA·소상공인 적재 → 상호명·면적 프리필 ← **파이프라인(C)**

2 를 먼저 해도 동작한다(팀이 넣은 상호명에 붙는다). 다만 3 이 들어와야 값이 산다.

### I. 소상공인 상가정보 (data.go.kr 15083033) — **적재 끝(09-05)** 554,092

분기 · **이용허락 제한 없음** · 키 불필요. 서울 554,092 업소 · 층 68.0% · 건물관리번호 99.6%.
**건물관리번호 앞 19자리 = PNU** → 층 있는 것 중 **88.8%** 가 우리 건물에 붙는다.
`master.buildings_v6` 에 **pnu 인덱스가 없다** — 조인 전에 둔다.

### K. LOCALDATA 인허가 — **적재 끝(09-05)** 3,146,365

`scripts/localdata/download_localdata.py` · 195업종 · 매일 · **이용허락 제한 없음**
서울 일반음식점 538,191행(영업중 120,220) · **층 80.7% · 면적 99.8% · 좌표 98.6%**
**소상공인에 없는 칸이 둘** — `소재지면적`(층별 계약면적 재료)과 `인허가일자·폐업일자`(시계열).
삼성동 78 에서 소상공인 2곳 대 이것 6곳.
**함정:** 기본 UA 면 403(헤드리스 크롬도 403·curl 은 된다) ·
`/file/download/{slug}/info?orgCode=6110000_ALL` 이 맞는 길(`/file/{slug}/download` 는 500) · cp949

### J. 업체 명부 셋을 다 쓰는 이유

서울 6개 건물로 맞대 보니 카카오와 소상공인의 겹침이 **23~28%** 뿐이다. 같은 건물을 봐도 서로 다른 업체를 잡는다.
카카오는 건물명·주차장 같은 시설과 최근 업체를, 소상공인은 고층 사무실을 층까지 잡는다.
이름 표기가 달라 겹침이 실제보다 낮게 잡히기도 한다(「필라테스더밸런스 차병원사거리점」 vs 「필라테스더밸런스차병원」).

---

## M. 상권을 **대장 용도** 대신 **실제 업체**로 — **적용(09-06)** · 갱신은 남음

**한 것(09-06)** — `backend/app/core/market.py` `STORES` 한 조각을 `buildings.py` `/pop`(건물·나대지) 과
`generate_report.py` `_market_zones` 가 끼운다. 정규식 두 벌은 지웠다. 갈래 일곱 · 색 `ZONE_COLOR` 일곱.
삼성동 78: 업무 46 · 판매 17 · 먹자 12 · 생활서비스 12 · 교육 8 · 의료 3 · 유흥 2.
**안 한 것** — `_use_type` 의 상권 프로필(활용 유형 분류 입력)은 대장 용도 그대로. `building_score` 배치와 같이(C).
갱신 자리 — **생김(09-07)**: `stores.localdata`(월)·`stores.sbiz`(분기) 단위. 남은 건 M-③(`_use_type`)뿐.


### 지금 무엇을 쓰고 있나

`master.floor_outline.use`(건축물대장의 층별 용도)를 정규식으로 넷으로 가른다. **두 곳에 같은 식이 박혀 있다.**

- `backend/app/domains/buildings.py` `_CAT` (유동인구 지도 격자 색)
- `backend/app/jobs/generate_report.py` `_market_zones` (보고서 상권 존 폴리곤)

### 왜 갈아야 하나 — 절반이 「기타」다

```
대장 층별 용도로 가른 서울 982,848개 층
  기타 499,001 (50.8%)   업무 236,860   판매 140,629   먹자 102,247   유흥 4,111
```

대장 용도는 **지을 때 신고한 것**이라 지금 그 층에 뭐가 있는지를 말하지 않는다.
「제2종근린생활시설」 한 낱말에 미용실·학원·의원·사무실이 다 들어간다.

### 실제 업체로 가르면 (소상공인 상권업종 중분류로 실측 · 서울 375,376건)

```
먹자 93,024(24.8%)  판매 70,968(18.9%)  업무 63,218(16.8%)  유흥 12,641(3.4%)
기타 135,525(36.1%)
```

기타가 절반에서 36% 로 준다. **그런데 남은 36% 를 열어 보면 갈래가 모자란 것이지 잡동사니가 아니다.**

```
이용·미용 24,438 · 기타 교육 17,616 · 의원 12,506 · 일반 교육 10,070
유원지·오락 7,314 · 기술 서비스 7,082
```

### 확정할 것 — 갈래를 몇으로 하나

넷은 이 데이터에 안 맞는다. 미용실 2.4만·학원 2.8만·의원 1.3만이 갈 곳이 없다.
**제안: 일곱** — 먹자 · 판매 · 업무 · 유흥 · 생활서비스(미용·세탁·수리) · 교육 · 의료.
지도 색이 넷에서 일곱이 되므로 [색-체계](../specs/02-screens/색-체계.md) 를 같이 본다.
**대표 확정 필요.**

### 갱신

지금은 갱신이라는 개념 자체가 없다(대장은 파이프라인이 다시 돌 때 같이 온다).
업체는 다르다 — **LOCALDATA 매일 · 소상공인 분기.** 새 판을 받아 갈아끼우는 자리를
파이프라인 derive 단계에 만든다. 폐업일자가 있으므로 **없어진 가게는 지운다**
(소상공인에는 폐업 칸이 없어 LOCALDATA 가 이 일을 한다).

### 순서

1. I·K 적재 (항목 C 에서 같이) — 이게 없으면 아무것도 못 한다
2. 갈래 확정 (일곱?) → 업종코드→갈래 표를 한 곳에 둔다. **두 곳에 박은 정규식을 지운다**
3. `_CAT`·`_market_zones` 를 그 표로 갈아끼운다
4. 갱신 자리를 파이프라인에 붙인다


---

## N. 대표 지시 (2026-09-05) — 화면 손보기

### N-1. 모달 두 개가 깨져 있다
- **업무탭 일정 모달** 이상함
- **매물 보류사유 모달** — 같은 이유. 글자 정렬이 어긋나고 **토스 결과 안 맞는다**
  (정본은 통합 매물 모달 `frontend/src/features/sales/draft/`)
- **보류사유가 매수자 모달과 연동이 안 된다** — 같은 테이블을 쓰는지부터 확인

### N-2. 「혹시 잊으셨나요」 드래그
잘못 끌면 **일정이 다음 날로 밀린다.** 드롭 판정이 틀렸다.

### N-3. 매수자 조건 → 건물검색
조건을 누르면 **그 조건으로 검색이 걸려야** 하는데 화면만 건물검색으로 넘어간다.

### N-4. 스펙명이 화면에 나와 있다
필터에 「S01 필터」가 span 으로 붙어 있다. 우리끼리 쓰는 코드는 화면에 안 쓴다.

### N-5. 실거래 — 연도 고르기
지금은 전체·1년·3년·5년뿐. **밸류맵처럼 1990~올해 중 고른 해만** 보이게.
**정할 것:** 검색 필터의 「실거래일」 조건과 겹친다. 둘을 하나로 합칠지, 차트 쪽은
보기 범위(view)로만 두고 필터는 그대로 둘지 — 대표 확정 필요.
(추천: 차트는 **보기 범위**, 필터는 **거르기**로 성격을 갈라 둘 다 남긴다.
 같은 값을 두 곳에서 고치게 하면 어느 쪽이 이겼는지 아무도 모른다.)

### N-6. 실거래 막대차트 — 단위가 섞여 있다
- 막대는 **평단가** 기준인데 메인 숫자는 **총액**이라 헷갈린다
- **연면적 평단가·대지면적 평단가를 둘 다** 보이게
- 건물상세 실거래 탭과 **건물검색 사이드탭** 둘 다

### N-7. 임대 금액 — 이름과 단위
- 보증금·임대료·관리비는 전부 **월 단위**다 → **「월 보증금」·「월 임대료」·「월 관리비」**
- **임대료·관리비가 억 단위로 찍힌다. 만 단위라야 한다**

### N-8. 업무탭 상태값이 화면과 안 맞는다 — **끝냄(09-05)** · `words.ts` `DEAL_TAIL`/`dealTail()` 한 곳
계약완료든 계약예정이든 화면엔 다 「이서연과 125억에 계약」으로 나온다.
**계약종료인데 「계약 예정」으로 뜬다.**
낱말을 그때그때 짓지 말고 **상태 → 표시 문구 표를 한 곳에** 두고 화면은 그 표만 읽는다.


---

## 원천 적재 — **끝냈다(2026-09-05)**

파이프라인 재적재(항목 C)와 **따로** 돌렸다. 기존 마스터를 안 건드리고 **새 테이블만 만들어 넣었다.**

| 테이블 | 줄 | 무엇 | 만든 것 |
|---|---|---|---|
| `master.localdata_permit` | 3,146,365 | LOCALDATA 서울 208업종. 영업 980,919 · 폐업 1,965,397 | `scripts/load_localdata.py` |
| `master.sbiz_store` | 554,092 | 소상공인 상가정보 서울 | `scripts/load_sbiz.py` |
| `master.urban_notice` | 43,986 | 고시 본문 43,932 · 고시문 PDF 17,074 | `scripts/load_urban_notice.py` |
| `ref.biz_category` | 75 | 업종 → 상권 7갈래 사전 | `db/migrations/0157` |

- `master.area_event` **1,625/1,787(90.9%)에 고시 본문**이 붙었다. 고시문 PDF 주소도 1,562.
  **이름 매칭을 버리고 고시번호로 붙였다** — `redevel_zone.label` 의 절반은 이름이 아니라 사업 종류라
  「재개발」 한 낱말이 고시 6,634건에 걸린다. 이름으로는 44개, 고시번호로는 **711개**가 붙는다.
- 이름 자리에 고시번호가 있던 291줄을 **고시 제목으로 갈았다**
  (「서울특별시 고시 제2026-341호 정비구역」 → 「미아동 345-1번지 일대 주택정비형 재개발사업」).
- `master.buildings_v6(pnu)` **인덱스를 놓았다** — 없어서 업체를 건물에 붙일 때마다 전수 훑기였다.
- 소상공인 층 있는 367,619건 중 **325,349(88.5%)가 우리 건물에 붙는다.**

### 상권 7갈래 — 실측으로 확인

기타가 **50.8% → 36.1% → 1.9%** 로 내려간다.

```
대장 층별 용도(지금 화면)   기타 50.8%
실제 업체 · 4갈래           기타 36.1%
실제 업체 · 7갈래           먹자 25.6 · 업무 25.5 · 판매 18.7 · 생활서비스 11.6
                            교육 8.3 · 유흥 4.8 · 의료 3.6 · 기타 1.9
```

숙박(4,836)과 도서관·사적지는 갈 칸이 없어 기타로 뒀다. 갈래를 늘리려면 `ref.biz_category` 한 줄이다.

### 받아만 두고 아직 안 넣은 것

- 없음(09-06). 나라장터는 `master.g2b_bid` 4,763줄로 들어갔다.

---

## 끝낸 것 (2026-09-06)

- **소식 탭** — 상단 GNB 「소식」 · `/news` · `GET /news`(`backend/app/domains/news.py`).
  `area_event` + `urban_notice`(area_event 에 안 붙은 것) 를 합쳐 날짜순, 커서는 「날짜|src_key」.
  갈래 칩 여섯(정비·개발 · 기반시설 · 건축 인허가 · 규제 · 정책 발표 · 고시·공고) + 이름 검색 pill.
  **자리를 모르는 것도 전부 저장한다** — 주변 소식은 자리 아는 것만 반경으로 자르고, 소식 탭은 안 자른다. 같은 자료다.
- **보도자료 자리 하나 = 한 줄** — `press_event` 2,039줄(기사 1,575 · 좌표 580 · 지번 231).
  `area_event` 열쇠는 기사번호가 아니라 줄 id(기사번호로 두면 커서가 같은 기사의 나머지 줄을 건너뛴다).
- **주변 소식 갈래 다섯** — 「기타」 대신 **규제**(개발행위허가제한 127). 「주변 동향」 → 「주변 소식」.
  날짜 라벨은 출처대로 발표일/공고일/고시일. 반경은 갈래마다(건축 인허가 250m).
- **해시태그**(09-06 대표 지시) — 한 줄엔 자리 하나만 보이니 그 글이 말하는 나머지를 `press_event.tags` 로 단다:
  주제(신속통합기획·모아타운·재건축…) · 자치구 · 법정동(3자 이상) · 역 · 도로. 같은 글의 줄은 같은 태그.
  `fetch_press.py --retag` 이 아는 글의 본문을 다시 읽어 채운다(본문은 안 남긴다). 본문은 `id="viewTable"` 칸만 —
  페이지 통째로 보면 바닥글 「중구 세종대로」가 모든 글에 #중구 를 단다.
  화면: 소식 상세·주변 소식 상세에 `#창동역` 꼴, 누르면 `/news?tag=창동역`.
- **소식 기간 칩** 1년(기본)·3년·5년·전체 — `GET /news?years=`. 「자리 안다/모른다」 줄과 점은 뺐다(동사꼴 라벨 금지).
- **지도 아이콘** — 사이드바 지도에 주변 소식을 찍는다(`PhotoPanel events=`). 기본은 **빅 이벤트만**
  (정비·개발 크레인 · 학교 · 철도 · 공원 · 신축 · 철거 · 공사 발주 · 정책 발표), 「소식 전부」를 누르면 기타(깃발)까지.
  목록에서 고르면 핀이 파랗게 차고 파문, 핀을 누르면 목록이 그 줄로(`useAreaPick`). 규칙은 `eventIcon.ts` 한 곳.
- **L. 층별 임대에 업체 원장** — 위 L 절. `qa/screen` 표본 「층별 임대 · 원장 상호명」.
- **층별 임대 · 업체마다 한 줄**(09-06 대표 지적 「외 3 으로 뭉개지고 업체별 상태·금액을 못 넣는다」) —
  팀 줄이 없는 층은 원장 업체 하나가 호실 하나로 미리 선다(상호명·면적·업종·개업·전화). 각 줄에 임대상태·금액을 따로 넣고,
  하나라도 고치면 그 층 줄 전부가 팀 줄이 된다. 팀 줄이 먼저 있던 층은 아래 업체 줄의 ＋ 로 호실을 세운다.
  **호실 면적은 LOCALDATA 가 아는 업체만**(영업 중 29.4% · 음식점·카페·미용 ~100%, 통신판매·의원 0%). 업체가 하나뿐이라도
  층 면적을 그 호실에 넣지 않는다(대표: 「총면적으로 미리 채워두는 건 구라」). 층 총면적은 대장 바닥면적으로 머리에 따로.
- **지역 검색이 지도를 안 옮기던 것**(09-06 대표, 옛 할일) — 지도는 첫 화면 자리(강남)에서 만들어지고, 지역을 골라도 핀만 바뀌고 지도는
  안 갔다. 게다가 자동완성으로 동을 고르면 지역 필터가 안 걸려 핀은 「내 매물」뿐이라 빈 동네가 섰다.
  고침: 자동완성 지역 고르면 그 동이 **지역 필터**가 되고(`Suggestion.bjd_code`), 지역·영역이 바뀌어 매물이 오면 지도가 그 상자에 **맞춘다**
  (`centerReq.bounds` → `fitBounds`, 왼쪽 패널만큼 비움). 정렬·필터만 바뀌면 안 움직인다. 상계동 → 중심 37.664,127.070 · 3,000건.
  `window.__btMap` 은 화면 검사용 지도 손잡이.
- **빈 값으로 나가면 지운 것**(09-06 대표 「값을 지우면 지운 상태로 저장돼야 하는데 되돌아간다」) — 클릭-편집 부품 넷이 빈 입력을
  「저장 안 함」으로 삼켰다. 규칙: **빈 칸으로 나가면 지우기.** 되돌리기(↺)가 있는 칸(대장 덮어쓰기)은 그것이 곧 지우기(대장값으로),
  없는 칸은 빈 값(null·0)을 저장. `KV.commit`·`NumCell`·`InfoRow.TextRow`·`FloorRows.Money`·통합 모달 `editable/mchip`.
  돈 칩(계약금·중도금)은 ✕ 가 따로 있어 빈 값은 그대로 둔다.
- **호실 번호는 모르면 비운다**(09-06 대표 「호실번호는 모르는 건데 왜 채우는겨」) — 0160: (건물·팀·층·호실) 유일키를
  **호실이 적힌 줄에만** 거는 부분 색인으로. 빈 호실 줄은 한 층에 여럿이어도 되고, 줄은 `id` 로 고친다(`PUT` 에 `id`).
  원장에서 세운 줄·＋호실·＋층 전부 번호 없이 선다. 호실 칸은 팀 줄에만 보이고 팀이 적는다.
- **호재 증분 전환** — 주간 크롤(고시 3쪽 · 나라장터 30일)이 표를 줄이던 함정을 막았다.
  `load_urban_notice.py` **DROP → upsert**(코드로) · `pdf_text` 등 고시문 글자 칸을 지킨다 · `pdf_tried` 로 안 받아 본 것만 받는다(`--retry`).
  `fetch_bids.py` 는 있던 파일과 공고번호로 **합쳐 쓴다**(5,241줄→유일 4,698 + 새 71 = 4,763). `run_pipeline.sh` 에 「고시문 글자」 단계 추가.
  **밟은 함정** — 오늘 낮 옛 로더가 표를 지워 고시문 글자 1,055건이 날아갔다. 다시 받는 중(`fetch_notice_pdf.py`).
- **보도자료 제목은 원문 그대로**(09-06 대표) — 제호는 저작물이 아니다(대법원 77다90 이래 일관). 4유형 조건은 본문에 붙는다.
  `press_event.title` 신설, `name = title`(없을 때만 지은 이름). 부제는 안 싣는다. `fetch_press.py --retag` 이 제목도 채운다.
- **소식 탭은 글 하나에 한 줄** — 자리마다 줄을 세운 보도자료(창동역 줄·종로5가역 줄)가 소식 탭에 둘로 섰다(대표 지적).
  `GET /news` 가 보도자료를 `press_event` 에서 `DISTINCT ON (ntt_no)` 로 따로 싣고 area_event 의 보도자료 줄은 뺀다.
  자리 모르는 글(1,459)도 이제 소식 탭에 선다. 주변 소식(반경)은 자리마다 줄 그대로.
- **찾기는 이름·내용·태그 전부** — `q` 가 name/body/tags ILIKE. 조건을 원천마다 안으로 밀어 넣고 trgm GIN 색인
  (`urban_notice` body·title · `area_event` name·body, 두 스크립트 DDL 에도) → 0.48초에서 수십 ms.
- **주변 소식도 기본 1년** — `AreaEvents` YEARS 1·3·5·전체. qa 표본은 「입지 → 전체」 두 번 누른다(일곱째 칸이 튜플).
- **이름 자리표시 청소** — 포털 제목이 「(기구축내용없음)」인 옛 고시 164건이 주변 소식에 그 글자 그대로 섰다(대표가 볼 화면).
  `build_area_event.py` 가 법정 표기(「도시계획시설(도로) 결정」·「개발행위허가 제한지역 지정」)로 부르고, 「제0000-000호」는 번호로 안 쓴다.
  이름·고시번호 둘 다 없는 정비구역 6건은 사업 종류(「재정비촉진지구」)로. `qa/data` 에 「이름 자리표시 없음」 불변식.
- **호재 잔업 중 안 한 것** — 나라장터 공고 **소재지**: 목록 API 에 현장 주소 칸이 없고 상세는 공고문(HWP) 안에만 있다. 자리 5.1%(242) 그대로.
  **겹치는 이름 114** — 실측하니 중복이 아니라 「거여마천 재정비촉진계획 변경결정」처럼 **같은 제목의 다른 고시**(2012·2013·2017 변경)다. 그대로 둔다.
- `qa/screen` 표본 둘(주변 소식 갈래 · 소식 갈래 칩) · `qa/ui` 는 소식 검색 pill(`q-pill`)을 이름으로 뺀다.
- 검사 `qa/run.sh screen ui` 15 ✓ · 0 ✗. 배포 안 함.

## 끝낸 것 (2026-09-05)

- **E-1. 호재 표** — `master.area_event` **1,787줄**(지구단위계획 948 · 정비구역 839).
  `scripts/build_area_event.py`. 사건 하나가 한 줄이다 — 건물×사건으로 펴면 1,000만 줄이 넘어 못 쓴다.
  이름 1,781 · 고시일 1,192 · 연도 1,736 · 고시번호 812
- **E. 주변 소식 화면** — 입지 탭 목차에 붙었다(`lc-ev`). `AreaEvents.tsx` + `GET /buildings/{pk}/events`.
  왼쪽 목록(갈래별 · 이름 · 날짜) 오른쪽 상세(위치 · 고시일 · 고시번호 · 내용 · 출처 · 고시문).
  목록 날짜는 `25.01`, 상세는 `2025년 1월 9일`. **연도만 아는 것은 `11년`** 으로 둔다(정비구역 839 중 고시일자는 273뿐).
  거리 칸은 안 만들었다 — 「구역 안」과 「78m」이 한 칸에 안 들어간다. 위치는 상세가 말한다.
  동향이 없는 자리면 카드도 목차 줄도 안 세운다
- **D. 층별 임대 UI 재설계** — 안C(목록＋상세) 적용 `3d03dc0`. 다섯 안 중 대표가 고름
- **D-1. 한 줄 배치 깨짐** — 금액 칸 104→72px, 상호명만 늘어나게. 열 머리 신설 `188050d`
- **F. 건폐율·용적률 중 하나가 안 고쳐짐** — 한 줄에 붙여 두고 용적률만 열려 있었다. 두 줄로 가름
- **층별 임대 입력이 추정을 덮던 것** — 보고서 `_nearby_rent_apply` 가 팀 줄로 층 추정을 갈아끼웠다.
  삼성동 78 1층이 0 으로 덮여 빈칸이었다 `9fb3f71`
- **상태 칸이 안 보이던 것** — 미지정일 때 글자를 안 그렸다. `—` 로 자리를 보이고 칩 둘로
- **G. 금액 억 단위 입력** — **이미 돼 있었다.** `parseAmount` 가 `1500`·`1500.1`·`15억5000만` 을 다 읽는다
- **H. 중개사 피드백 백로그 재대조** — 「안 됨」 열 개 중 **여섯이 이미 돼 있었다.**
  기록만 낡았다. 정본은 메모리 `broker-feedback-backlog`
- **V-World 라이선스** — 표기는 `CC BY-NC-ND` 지만 **진흥원 확인 결과 상관 없다**(대표 통화).
  막히는 것은 서울시 건설공사 추진현황(OA-2540, 공공누리 4유형) 하나

## 안 하기로 한 것

- 관리비 지출 · 월수익
- 검색 리스트에서 적합/부적합/제외
- **보도자료 본문**(서울시 이용허락 요청 안 함 · 제목·사실·태그·링크로 충분) — 2026-09-06 대표
- **나라장터 공고 소재지**(목록 API 에 칸 없음 · 자리 아는 5% 그대로) — 2026-09-06 대표
- **주변 소식 겹치는 이름 114**(같은 제목의 다른 고시라 중복 아님) — 2026-09-06 대표

## 2026-09-04 에 끝낸 것

1. **건물정보 승강기는 대장 기준** — 코드 완료, 값은 C 에서 갈린다.
   빌더 `_elev`(대장 전용)·`_elev_ext`(공단) 분리 · 0156 `elevator_ext` · API `_ref` · 화면 참조 곁말.
   건폐율·용적률도 대장이 비면 계산값을 옆에 참조로.
2. **평·㎡ 토글에 공시지가가 따라온다.** GongsiCard·GongsiTable 이 unit 을 받는다. 기본 평.
3. **층별 임대정보**
   - 용도 제거, **상호명** 신설(0155 `tenant_name`)
   - 순서 1층 → 위 → 옥탑 → 지하 (FloorRows `frank` · floor_rents ORDER BY · RentPanel `floorRank`)
   - 「＋ 호실」이 한 호실짜리 층에 안 뜨던 것 고침
   - **호실 번호를 지어내지 않는다.** 대장은 호실을 안 주므로 대장 줄에는 호실이 없고,
     한 호실뿐인 층도 호실을 안 보여준다. 팀이 ＋호실로 나눌 때만 101·102 로 선다.
   - **`sfloor` 버그**: 「옥탑1층」이 「1층」과 같은 층으로 읽혀 1층이 「102호」로 떴다. 옥탑을 따로 센다.
   - 여러 호실 층의 접기 제거. 항상 펴져 있다.
   - 입력 카드: 상호명·면적·상태 한 줄, 호실은 그 아래.
4. **「10년 전 대비 +5%」**
5. **「법정 대비 건폐 · 용적」** = 현재 − 법정. 넘으면 + 이고 빨강. **소수점 1자리.** `%p` 전부 제거.
6. **주변시세 막대 호버** — 막대 전체에 「이 건물 … · 주변 시세 …」, 눈금은 잡는 영역을 넓혔다.
7. **지도 검색 엔터 이동** — 자동완성이 180ms 늦어 헛돌던 것. 친 글자로 그 자리에서 묻는다.
8. **「도산대로 · 오피스 요율로 역산」 스판 삭제**
9. **안내 스판 전부 삭제** — 「매매가를 넣으면 섭니다」 외 여섯.

## 앞선 작업

- 계약서·확인설명서 원본 골격 이식(명도·승계·포괄양수도 · 확인설명서 4쪽). 커밋 전.
- 건물 상세 임대 탭 「업체 정보」(카카오 로컬). 커밋 `ba804f6`, 개발서버 배포됨.

## 미정

- 소상공인 상가(상권)정보로 앵커 테넌트(스타벅스 등) 신호. 카카오 자료는 저장 못 하니 이 원장으로.
- 국민연금 사업장 데이터: 주소가 **법정동까지만** 온다(실측 2026-09-04). 건물 단위 불가, 동 단위 추세로만.
