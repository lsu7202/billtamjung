# 매뉴얼 영상 내레이션 — 재녹음 대기

타입캐스트 크레딧 소진(402)으로 아래를 못 뽑았다. 충전 후 `gen_manual_vo.py`의 해당 줄만 다시 생성.

- **r2** 현재 음성이 "…첫 장에 정리되고," 로 끝나 다음 문장을 기다리는 억양이다.
  PPT 안내(r3)를 폐지하면서 r2가 마지막 문장이 됐으므로,
  "적정가와 예상수익률, 매력도 등급이 첫 장에 정리됩니다." 로 재녹음할 것.
- 재녹음 후 `python3 normalize_vo.py` → `npx remotion render Manual ...` 로 영상 재생성.
