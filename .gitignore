# macOS
.DS_Store

# 임시/렌더 산출물
*.tmp
_*.html
*.png.crop
scratchpad/

# 아카이브·복제본 (버전관리 제외)
*.zip
index copy.html

# 원본 데이터 (대용량, git 제외)
data/raw/
__pycache__/

# 백엔드/프론트 (2026-07 구현)
**/.venv/
*.pyc
.env
.env.*
!.env.example
**/node_modules/
frontend/dist/
frontend/.vite/
frontend/tsconfig.tsbuildinfo
data/*.db
scripts/vworld/cookie.txt
scripts/deploy/.env.deploy
backups/*
.playwright-mcp/
.firebase/
*.hwpx.bak
firebase-debug.log
data/_raw_backup_*.tar.zst
data/*.db.bak
# 빌드 산출물 — 빌더를 돌리면 다시 나온다. 원본(data/raw)에서 재생성되므로 커밋하지 않는다.
# gitignore 는 줄 끝 주석을 모른다(#도 경로의 일부다) — 설명은 반드시 윗줄에 둔다.
# build_floor_outline.py · 253MB
data/tools/_floor_outline.csv
# build_road_width.py(도로구간 shp) · 22MB
data/tools/_road_width.csv
# build_expos.py · 1.9GB
data/tools/_expos.jsonl
# build_complex.py · 15MB
data/tools/_complex.jsonl
# build_building_master.py
data/tools/_building_master.jsonl
# build_daesuseon.py · 10MB
data/tools/_daesuseon_seoul.json
# build_annex.py
data/tools/_annex_*.json
# build_integrated.py
data/tools/_integrated.jsonl
# build_land_master.py
data/tools/_land_master.jsonl
# 2026-08~09 신설 빌더 산출물 — 합쳐 5GB. 원본 압축본에서 다시 만든다.
# gitignore 는 줄 끝 주석을 모른다(#도 경로의 일부다) — 설명은 반드시 윗줄에 둔다.
# build_aptprice.py · 1.9GB
data/tools/_aptprice.jsonl
# build_basic.py · 2.2GB
data/tools/_basic.jsonl
# build_closed.py
data/tools/_closed.jsonl
# build_energy.py
data/tools/_energy.jsonl
# build_septic.py
data/tools/_septic.jsonl
# build_zone.py
data/tools/_zone.jsonl
# build_road_width.py
data/tools/_road_width.csv

# 빌드 결과서(data/tools/_reports/)는 **남긴다** — 무엇을 어떻게 다뤘는지가 기록이다

# 매뉴얼 영상 중간산출물 — 원본에서 재생성 가능(복제본이라 이중 보관 방지)
ad/remotion/public/shots/          # = manual/img/*.png 복사본
ad/remotion/public/vo/raw/         # 정규화 전 원본(gen_manual_vo.py로 재생성)
scripts/.pipeline.env
qa/screen/.qa_screen.env
!backups/README.md

# 광고·가이드 매체 — 영상·음성·프레임 PNG 는 git 에 안 둔다(2026-09-02). 소스(remotion/src·*.py)만 추적.
ad/원본/
ad/완성/
ad/capture/out/
ad/remotion/public/
ad/*.mp4
ad/*.mp3
# gitignore 는 줄 안 주석이 안 된다 — 주석은 윗줄에(2026-09-02 그렇게 썼다가 안 먹음)
# ad/capture/assets/*.png : 예시-첨부물/ 과 같은 7장. 정본은 예시-첨부물/
ad/capture/assets/*.png
# data/eum_check*.csv : 토지이음 대조 결과. qa/external 로 다시 만든다
data/eum_check*.csv

# 채용 지원 서류 — 개인정보(전화번호·사진) 포함, 올리지 않는다
docs/지원/
